<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class CacheBackupSeeder extends Seeder
{
    /**
     * Backup seed for the cache table.
     * Data backed up on: 2026-04-30
     * Note: Cache is framework-managed and typically regenerated at runtime.
     */
    public function run(): void
    {
        // Cache entries are transient and auto-managed by Laravel.
        // This seeder exists as a structural backup.
        // No data to restore.
    }
}
